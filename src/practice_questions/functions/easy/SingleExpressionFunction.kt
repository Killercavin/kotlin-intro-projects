package practice_questions.functions.easy

/*
Single Expression Function: Convert this function to a single-expression function

fun singleExpressionFunction(x: Int): Int {
    return x * 2
}
 */

fun singleExpressionFunction(x: Int): Int = x * 2

fun main() {
    println(singleExpressionFunction(10))
}